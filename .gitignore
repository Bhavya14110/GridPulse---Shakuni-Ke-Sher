__pycache__/
*.py[cod]
.venv/
venv/
.env
.env.local
*.db
backend/ml/data/
# The trained models ARE committed: they are the deployable artifact, a
# deployment has no opportunity to run the training script, and together they
# are under 10 MB. The dataset they were built from stays out.
backend/ml/saved_models/*.joblib
node_modules/
.next/
out/
.DS_Store
dev.log
*.log
next-env.d.ts
*.tsbuildinfo

# Stray archives / downloads
*.zip
