# Extra dependencies for building the dataset and training the models.
#   pip install -r requirements-train.txt
#
# scikit-learn is only used to score a training run, never to serve a forecast,
# so keeping it out of requirements.txt keeps ~100 MB out of the deploy bundle.
-r requirements.txt
scikit-learn==1.6.0
