# Extra dependencies for building the dataset, training and verifying the models.
#   pip install -r requirements-train.txt
#
# Neither of these is imported when serving a request: scikit-learn only scores
# a training run, and xgboost is replaced at inference time by the numpy tree
# walker in app/services/tree_model.py. Keeping them out of requirements.txt
# keeps roughly 600 MB out of a deployment bundle -- xgboost's Linux wheel alone
# is ~154 MB compressed and pulls scipy with it.
#
# Run ml/verify_tree_model.py after training to confirm the numpy scorer still
# agrees with xgboost before shipping.
-r requirements.txt
xgboost==2.1.3
scikit-learn==1.6.0
