# GridPulse backend — runtime dependencies. Python 3.10+.
#   pip install -r requirements.txt
#
# This is also what a deployment installs, so it holds only what serving a
# request actually needs. Notably absent: xgboost and scikit-learn. Training
# needs them; serving reads the saved JSON model with numpy instead, which keeps
# ~600 MB out of the bundle. See requirements-train.txt.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pandas==2.2.3
numpy==2.2.1
requests==2.32.3
apscheduler==3.11.0
