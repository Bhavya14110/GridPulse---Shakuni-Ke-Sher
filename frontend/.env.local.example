# Next.js only reads NEXT_PUBLIC_* vars from its own directory, so this mirrors
# the root .env. Copy to .env.local if your API isn't on localhost:8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
